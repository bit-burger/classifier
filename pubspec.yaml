name: classifier
description: Classifiying images

version: 1.0.0+1

environment:
  sdk: ">=2.7.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter

  cupertino_icons: ^0.1.3
  tflite: ^1.1.2
  image_picker: ^0.6.7
  image: ^2.1.4

dev_dependencies:
  flutter_test:
    sdk: flutter

flutter:

  uses-material-design: true

  assets:
    - assets/labels.txt
    - assets/mobilenet_v1_1.0_224.tflite
